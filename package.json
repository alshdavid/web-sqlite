{
  "name": "@alshdavid/sqlite-web",
  "type": "module",
  "version": "0.0.1",
  "description": "This is a basic implementation of a persistent cross-tab sqlite database for the browser",
  "main": "index.js",
  "files": [
    "package.json",
    "index.js",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf ./dist && node ./build.mts"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
